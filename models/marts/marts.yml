version: 2

models:
  - name: fct_prompt_responses
    config:
      tags: "ivr_prompt"
    columns: 
       - name: prompt_response_uuid
         description: "The primary key for this table"
         tests:
           - unique
           - not_null

# slg
  - name: fct_activities
    config:
      tags: "slg"
    columns: 
       - name: activities_id
         description: "The primary key for this table"
         tests:
           - unique
           - not_null